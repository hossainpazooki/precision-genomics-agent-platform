name: precision-genomics
runtime:
  name: python
  options:
    virtualenv: venv
description: >-
  Infrastructure for the Precision Genomics Agent Platform.
  Claude-orchestrated multi-omics MSI classification on GCP.
config:
  gcp:project:
    description: GCP project ID
  gcp:region:
    default: us-central1
