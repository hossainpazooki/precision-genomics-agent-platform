"""GCP Workflows — REMOVED.

Workflow orchestration migrated to the TypeScript app layer
(web/src/lib/workflows/engine.ts). The YAML definitions that this
component loaded from workflows/definitions/ were deleted in commit
42700ec. This module is kept as a stub so existing imports don't break;
remove the import sites when convenient.
"""
